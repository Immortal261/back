# Зависимости — ставятся заново через npm install, в git не нужны
node_modules/

# Секреты: пароли администратора, ссылка на оплату и т.п. — НИКОГДА не коммитить
.env

# База данных с реальными заказами/остатками/клиентами — тоже не в git
# (это ваши боевые данные, а не код; для бэкапа используйте отдельное копирование файла)
server/data/shop.db
server/data/shop.db-shm
server/data/shop.db-wal

# Системные файлы
.DS_Store
*.log
